# Extra dependencies for the local backtest tool (src/signals/backtest.py).
# NOT needed by the server — install only when you want the equity.png chart:
#     pip install -r requirements-backtest.txt
matplotlib>=3.8.0
